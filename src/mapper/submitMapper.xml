<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="submitRepository">

    <insert id="insertSubmit">
        INSERT INTO Submit (stud_id, type, problemNo, code, lang)
        VALUES (#{studId}, #{type}, #{problemNo}, #{code}, #{lang})
    </insert>

    <select id="getSubmitByStudId" parameterType="int">
        SELECT *
        FROM Submit
        WHERE stud_id = #{studId}
    </select>

    <update id="updateSubmitScore">
        UPDATE Submit
        SET score = #{score}
        WHERE id = #{studId}
    </update>
    
    <select id="getSubmitScoreByStudId" parameterType="int">
        SELECT score
        FROM Submit
        WHERE stud_id = #{studId}
    </select>
</mapper>